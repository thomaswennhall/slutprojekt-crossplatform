import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   profileImage: {
      width: 80,
      height: 80,
      borderRadius: 50,
      marginRight: 30,
   },
   profile: {
      display: "flex",
      flexDirection: "row",
      padding: 32,
   },
   profileRole: {
      fontSize: 16,
   },
   profileHeader: {
      fontSize: 20,
      fontWeight: "bold",
   },
});

export default styles;
