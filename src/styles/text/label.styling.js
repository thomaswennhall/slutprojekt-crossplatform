import styled from 'styled-components/native'

export const Label = styled.Text`
  color: ${props => props.theme.colors.gray};
  font-size: 14px;
  font-family: ${'NotoSansJP_400Regular' || 'auto'};
`
