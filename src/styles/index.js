import {
  Container,
  SafeAreaView,
  FlatList,
  Portrait,
  TouchableAreaOpacity,
  TouchableAreaText,
} from "./utils";

export {
  Container,
  SafeAreaView,
  FlatList,
  Portrait,
  TouchableAreaOpacity,
  TouchableAreaText,
};
